import React from 'react';
import PropTypes from 'prop-types';
import Layout from 'components/publishing/layout/layout';
import './style.scss';

const Publishing = () => {
  return <Layout />;
};

export default Publishing;
